# 1. Tell Git your identity
git config --global user.name "osma146"
git config --global user.email "jeffhe0906@gmail.com"

# 2. Link your local folder to the GitHub repo
git remote set-url origin https://github.com/osma146/pepper_project.git

# 3. Make sure you’re on main branch
git branch -M main

# 4. Stage all files
git add .

# 5. Commit them
git commit -m "Initial commit"

# 6. Push to GitHub
git push -u origin main



git pull       # get latest changes from GitHub
git add .      # stage your changes
git commit -m "update"
git push       # send them to GitHub
